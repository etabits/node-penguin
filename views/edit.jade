extends layout

block content
	form.clearfix(method='post', action="", enctype='multipart/form-data')
		include p/form
		- formOpts.valueOverrides = conditions
		+form(form, formOpts)
		button.btn.btn-big.pull-right.btn-primary(type='submit')=t('Save')